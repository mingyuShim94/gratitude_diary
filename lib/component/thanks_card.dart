import 'package:flutter/material.dart';

class ThanksCard extends StatelessWidget {
  const ThanksCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
